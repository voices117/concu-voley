#ifndef UTILS_HPP
#define UTILS_HPP

/* include area */
#include <random>


namespace Utils {
    /** Generates a random number in the range [min, max] */
    int rand_int( int min, int max );
}


#endif
